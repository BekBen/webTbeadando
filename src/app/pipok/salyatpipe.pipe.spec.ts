import { SalyatpipePipe } from './salyatpipe.pipe';

describe('SalyatpipePipe', () => {
  it('create an instance', () => {
    const pipe = new SalyatpipePipe();
    expect(pipe).toBeTruthy();
  });
});
